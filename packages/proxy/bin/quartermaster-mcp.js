#!/usr/bin/env node
// quartermaster-mcp CLI entry (scaffold).
// Reads a config (servers + synonyms), builds the offline index, and serves a
// single `retrieve_tools` meta-tool over MCP stdio.
//
// Usage (planned):  quartermaster-mcp --config ./quartermaster.json
console.error('quartermaster-mcp: scaffold — proxy server not yet wired. See packages/proxy/src/index.ts');
process.exit(1);
