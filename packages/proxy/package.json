{
  "name": "quartermaster-mcp",
  "version": "0.1.4",
  "description": "Offline, zero-config MCP proxy: federate N downstream servers behind one ranked retrieve_tools. Self-contained — no embedding model, no network.",
  "type": "module",
  "main": "./dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "bin": {
    "quartermaster-mcp": "./bin/quartermaster-mcp.js",
    "quartermaster": "./bin/quartermaster.js"
  },
  "files": ["dist/index.js", "dist/cli.js", "bin", "README.md"],
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --target=node22 --external:@modelcontextprotocol/sdk --external:@modelcontextprotocol/sdk/* --loader:.json=json --outfile=dist/index.js && esbuild ../cli/src/index.ts --bundle --platform=node --format=esm --target=node22 --external:quartermaster-mcp --external:@modelcontextprotocol/sdk --external:@modelcontextprotocol/sdk/* --outfile=dist/cli.js",
    "prepublishOnly": "pnpm build",
    "test": "node --test --experimental-strip-types test/*.test.ts",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=22"
  },
  "keywords": ["mcp", "model-context-protocol", "proxy", "gateway", "tool-router", "tool-selection", "tool-retrieval", "bm25", "offline", "agents"],
  "license": "MIT",
  "author": "Pranav Nagrecha",
  "repository": {
    "type": "git",
    "url": "github:PranavNagrecha/quartermaster",
    "directory": "packages/proxy"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ajv": "^8.17.1"
  },
  "devDependencies": {
    "@quartermaster/core": "workspace:^",
    "@quartermaster/policy": "workspace:^",
    "@quartermaster/telemetry": "workspace:^",
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.5.0"
  }
}
