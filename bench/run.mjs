// Recall@K bench runner (scaffold).
// Loads labeled cases, runs each ranker over each manifest, reports recall@K.
// Fill in cases/ and wire @quartermaster/core once built (dist/).
//
// import { createRouter } from '../packages/core/dist/index.js';

console.log('bench: scaffold — add fixtures under bench/cases/ and implement run.mjs.');
console.log('Metric: recall@K (is the correct tool in the top K?). See docs/benchmarks.md.');
